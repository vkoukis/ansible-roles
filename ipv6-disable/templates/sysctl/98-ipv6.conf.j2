# Configure IPv6 on all interfaces
net.ipv6.conf.all.disable_ipv6 = {{ 1 if ipv6_disabled else 0 }}
net.ipv6.conf.default.disable_ipv6 = {{ 1 if ipv6_disabled else 0 }}
#net.ipv6.conf.lo.disable_ipv6 = {{ 1 if ipv6_disabled else 0 }}
